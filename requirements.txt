# ---- Stage 1 (computer vision) ----
torch>=2.0
torchvision>=0.15
numpy>=1.24
opencv-python>=4.8
pillow>=9.5
matplotlib>=3.7
scikit-learn>=1.3        # metrics: confusion matrix, classification report
tqdm>=4.65

# ---- Stage 2 (grading + digital twin + dashboard) ----
streamlit>=1.30
pandas>=2.0

# Stage 3 (ROS 2 / Gazebo / RViz) is installed via apt inside the ROS 2 Humble
# environment, not pip — see stage3 README (added in Stage 3).
